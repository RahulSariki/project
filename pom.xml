server.port= 9090

spring.application.name= config-server



eureka.client.service.url.defaultZone=http://localhost:8761/eureka
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
spring.cloud.config.server.git.uri=https://gitlab1.rpsconsulting.in/26SUB0734_U17/wipro-config-repo.git
spring.cloud.config.server.git.username=26SUB0734_U17@rpsconsulting.in
spring.cloud.config.server.git.password=Red@12345
spring.cloud.config.server.git.skip-ssl-validation=true
spring.cloud.config.server.git.default-label=main
spring.cloud.config.server.git.clone-on-start=true




spring.config.import=optional:configserver:http://localhost:8888



package com.BankSystem.ApiGatway.security;

import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.web.server.ServerHttpSecurity;
import org.springframework.security.web.server.SecurityWebFilterChain;

@Configuration
public class SecurityConfig {

    @Bean
    public SecurityWebFilterChain securityWebFilterChain(
            ServerHttpSecurity http) {

        return http
                .csrf(ServerHttpSecurity.CsrfSpec::disable)
                .authorizeExchange(exchange -> exchange

                        .pathMatchers("/auth/**").permitAll()
                        .pathMatchers("/customers/**").permitAll()
                        .pathMatchers("/accounts/**").permitAll()
                        .pathMatchers("/transactions/**").permitAll()
                        .pathMatchers("/beneficiaries/**").permitAll()
                        .pathMatchers("/loans/**").permitAll()
                        .pathMatchers("/notifications/**").permitAll()

                        .anyExchange().authenticated()
                )
                .build();
    }
}